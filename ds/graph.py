class node:

class graph:
	def __init__():
		self.g={}
	def add(node,edge):
		str(edge).remove(node)
		if node in self.g:
			self.g[node].append(edge)
		else:
			self.g[node]=[edge]
	#node:conections
	